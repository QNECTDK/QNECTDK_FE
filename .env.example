# Qnect 프론트엔드 환경변수 예시
# 이 파일을 복사해 .env 로 만든 뒤 값을 채우세요. (cp .env.example .env)
# Vite 규칙상 클라이언트에 노출되는 변수는 반드시 VITE_ 접두사가 필요합니다.
# 주의: 여기에 비밀키/토큰을 넣지 마세요. 빌드 결과물에 그대로 포함돼 노출됩니다.

# 백엔드 API 베이스 URL (미설정 시 코드 기본값 http://localhost:8080 사용)
VITE_API_BASE_URL=http://localhost:8080
